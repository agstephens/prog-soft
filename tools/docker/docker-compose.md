You'll need a `docker-compose.yml` file in the current directory, then you can do stuff like:

```
docker-compose up -d --build
docker-compose start
docker-compose stop
docker-compose ps
docker-compose logs-f
```